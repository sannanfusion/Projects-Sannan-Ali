Internet Speed Test (Python)

This is a simple Python script that tests your download speed, upload speed, and ping using the speedtest-cli library.

🚀 Features

Tests Download Speed (Mbps)

Tests Upload Speed (Mbps)

Shows Ping (ms)

Automatically picks the best server

📦 Required Module

You must install the correct library:

✅ Install speedtest-cli (NOT speedtest)

Run this in your terminal or CMD:

pip install speedtest-cli


If you installed the wrong module earlier, uninstall it:

pip uninstall speedtest


Then install again:

pip install speedtest-cli

📂 How to Run the Program

Save your script as main.py

Open CMD inside the project folder

Run:

python main.py